# Generated by `go run ./cmd/openapi-gen` from internal/server's route table
# (internal/server/openapigen parses handlers_*.go's Go source directly —
# see that package's doc comment for why: generic type parameters are not
# recoverable from a running binary). DO NOT EDIT BY HAND; regenerate
# instead. openapigen_test.go's TestGeneratedFilesAreUpToDate fails the
# build the day this file goes stale.
openapi: 3.0.3
info:
  title: blunderDB engine API
  version: "1"
  description: >-
    POST /v1/<family>.<method> over HTTP + JSON (NDJSON for a streaming
    list). SECURITY: this daemon performs NO authentication of its own — it
    trusts the X-Tenant-ID request header verbatim and MUST run behind an
    authenticating reverse-proxy (see ADR-0005 and doc/source/mode_headless.rst).
    X-Tenant-ID is the tenant's positive decimal integer; a name is refused
    with 400 invalid, never mapped to a tenant by this daemon. Idempotency:
    positions.save (and every other positions.* write) is naturally
    idempotent through its Zobrist content hash — saving the same position
    twice returns the same row, never a duplicate. collections.create,
    tournaments.create and anki.reviewCard have no such natural key (two
    calls are two distinct effects) and instead accept an optional
    Idempotency-Key request header (marked "x-idempotency-key: true" below):
    a retried call carrying the same key replays the first attempt's result
    instead of repeating its effect. Every other route neither needs nor
    accepts that header.
servers:
  - url: /v1
paths:
  /ops/maintenance.vacuum:
    post:
      operationId: maintenance.vacuum
      tags: [maintenance]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /ops/tenant.purge:
    post:
      operationId: tenant.purge
      tags: [tenant]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/analyses.delete:
    post:
      operationId: analyses.delete
      tags: [analyses]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/analyses.load:
    post:
      operationId: analyses.load
      tags: [analyses]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_PositionAnalysis"
                nullable: true
  /v1/analyses.repair:
    post:
      operationId: analyses.repair
      tags: [analyses]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/repairResp"
  /v1/analyses.save:
    post:
      operationId: analyses.save
      tags: [analyses]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/analysisSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.buryCard:
    post:
      operationId: anki.buryCard
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/cardIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.createDeck:
    post:
      operationId: anki.createDeck
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckCreateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/anki.deckPositions:
    post:
      operationId: anki.deckPositions
      tags: [anki]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/anki.deckStats:
    post:
      operationId: anki.deckStats
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_AnkiDeckStats"
                nullable: true
  /v1/anki.deleteDeck:
    post:
      operationId: anki.deleteDeck
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.forecast:
    post:
      operationId: anki.forecast
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/forecastReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/domain_AnkiForecastDay"
  /v1/anki.listDecks:
    post:
      operationId: anki.listDecks
      tags: [anki]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_AnkiDeck"
                nullable: true
  /v1/anki.nextCard:
    post:
      operationId: anki.nextCard
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_AnkiReviewCard"
                nullable: true
  /v1/anki.removeCard:
    post:
      operationId: anki.removeCard
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/cardIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.resetDeck:
    post:
      operationId: anki.resetDeck
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.retention:
    post:
      operationId: anki.retention
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/retentionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_AnkiRetention"
                nullable: true
  /v1/anki.reviewCard:
    post:
      operationId: anki.reviewCard
      tags: [anki]
      x-kind: json
      x-idempotency-key: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/reviewCardReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_AnkiReviewCard"
                nullable: true
  /v1/anki.reviewLog:
    post:
      operationId: anki.reviewLog
      tags: [anki]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/reviewLogReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_AnkiReviewLog"
                nullable: true
  /v1/anki.reviewsByGameType:
    post:
      operationId: anki.reviewsByGameType
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/reviewsByGameTypeReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: integer
                  format: int32
  /v1/anki.suspendCard:
    post:
      operationId: anki.suspendCard
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/suspendCardReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.sync:
    post:
      operationId: anki.sync
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.syncWithPositions:
    post:
      operationId: anki.syncWithPositions
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckSyncPositionsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.updateDeck:
    post:
      operationId: anki.updateDeck
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckUpdateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/anki.updateDeckParams:
    post:
      operationId: anki.updateDeckParams
      tags: [anki]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/deckParamsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.addPosition:
    post:
      operationId: collections.addPosition
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collPositionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.addPositions:
    post:
      operationId: collections.addPositions
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collPositionsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.collectionsOf:
    post:
      operationId: collections.collectionsOf
      tags: [collections]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/storage_Collection"
                nullable: true
  /v1/collections.copyPosition:
    post:
      operationId: collections.copyPosition
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/copyPositionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.create:
    post:
      operationId: collections.create
      tags: [collections]
      x-kind: json
      x-idempotency-key: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collectionCreateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/collections.delete:
    post:
      operationId: collections.delete
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.get:
    post:
      operationId: collections.get
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/storage_Collection"
                nullable: true
  /v1/collections.list:
    post:
      operationId: collections.list
      tags: [collections]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/storage_Collection"
                nullable: true
  /v1/collections.movePosition:
    post:
      operationId: collections.movePosition
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/movePositionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.positionIndexMap:
    post:
      operationId: collections.positionIndexMap
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: integer
                  format: int32
  /v1/collections.positions:
    post:
      operationId: collections.positions
      tags: [collections]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collectionPositionsReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/collections.removePosition:
    post:
      operationId: collections.removePosition
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collPositionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.removePositions:
    post:
      operationId: collections.removePositions
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collPositionsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.reorder:
    post:
      operationId: collections.reorder
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collectionReorderReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.reorderPositions:
    post:
      operationId: collections.reorderPositions
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collPositionsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.setFilter:
    post:
      operationId: collections.setFilter
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collectionFilterReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/collections.update:
    post:
      operationId: collections.update
      tags: [collections]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/collectionUpdateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/comments.add:
    post:
      operationId: comments.add
      tags: [comments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/commentAddReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/comments.byPosition:
    post:
      operationId: comments.byPosition
      tags: [comments]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_CommentEntry"
                nullable: true
  /v1/comments.delete:
    post:
      operationId: comments.delete
      tags: [comments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/commentIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/comments.deleteForPosition:
    post:
      operationId: comments.deleteForPosition
      tags: [comments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/comments.listAll:
    post:
      operationId: comments.listAll
      tags: [comments]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/listReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_CommentEntry"
                nullable: true
  /v1/comments.search:
    post:
      operationId: comments.search
      tags: [comments]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/commentSearchReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_CommentEntry"
                nullable: true
  /v1/comments.tags:
    post:
      operationId: comments.tags
      tags: [comments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/tagsResp"
  /v1/comments.text:
    post:
      operationId: comments.text
      tags: [comments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/textResp"
  /v1/comments.update:
    post:
      operationId: comments.update
      tags: [comments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/commentUpdateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/exports.json:
    post:
      operationId: exports.json
      tags: [exports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/exports.sqlite:
    post:
      operationId: exports.sqlite
      tags: [exports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/filters.delete:
    post:
      operationId: filters.delete
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/filters.list:
    post:
      operationId: filters.list
      tags: [filters]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/storage_Filter"
                nullable: true
  /v1/filters.loadEditPosition:
    post:
      operationId: filters.loadEditPosition
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/filterNameReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/textResp"
  /v1/filters.loadExcludePosition:
    post:
      operationId: filters.loadExcludePosition
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/filterNameReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/textResp"
  /v1/filters.save:
    post:
      operationId: filters.save
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/filterSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/filters.saveEditPosition:
    post:
      operationId: filters.saveEditPosition
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/editPositionSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/filters.saveExcludePosition:
    post:
      operationId: filters.saveExcludePosition
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/excludePositionSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/filters.update:
    post:
      operationId: filters.update
      tags: [filters]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/filterUpdateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/gammonnet.analyzeMissing:
    post:
      operationId: gammonnet.analyzeMissing
      tags: [gammonnet]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/gammonnet.analyzeMissing.cancel:
    post:
      operationId: gammonnet.analyzeMissing.cancel
      tags: [gammonnet]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/gammonnet.compare:
    post:
      operationId: gammonnet.compare
      tags: [gammonnet]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/gammonnet.cubeMatrix:
    post:
      operationId: gammonnet.cubeMatrix
      tags: [gammonnet]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/gammonnet.sweepStale:
    post:
      operationId: gammonnet.sweepStale
      tags: [gammonnet]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/history.clear:
    post:
      operationId: history.clear
      tags: [history]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/history.load:
    post:
      operationId: history.load
      tags: [history]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/stringsResp"
  /v1/history.save:
    post:
      operationId: history.save
      tags: [history]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/commandSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/imports.bgf:
    post:
      operationId: imports.bgf
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/imports.cancel:
    post:
      operationId: imports.cancel
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/imports.db:
    post:
      operationId: imports.db
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/imports.gnubg:
    post:
      operationId: imports.gnubg
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/imports.json:
    post:
      operationId: imports.json
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/imports.list:
    post:
      operationId: imports.list
      tags: [imports]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/listReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/domain_ImportBatch"
                  nullable: true
  /v1/imports.position:
    post:
      operationId: imports.position
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/imports.report:
    post:
      operationId: imports.report
      tags: [imports]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/importReportReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_ImportBatch"
                nullable: true
  /v1/imports.studyQueue:
    post:
      operationId: imports.studyQueue
      tags: [imports]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/importStudyQueueReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/domain_StudyQueueEntry"
  /v1/imports.xg:
    post:
      operationId: imports.xg
      tags: [imports]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/matches.createGame:
    post:
      operationId: matches.createGame
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/gameReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/matches.createMove:
    post:
      operationId: matches.createMove
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/moveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/matches.delete:
    post:
      operationId: matches.delete
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/matches.exportMat:
    post:
      operationId: matches.exportMat
      tags: [matches]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/matches.findByHash:
    post:
      operationId: matches.findByHash
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/findByHashReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/findByHashResp"
  /v1/matches.games:
    post:
      operationId: matches.games
      tags: [matches]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Game"
                nullable: true
  /v1/matches.get:
    post:
      operationId: matches.get
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Match"
                nullable: true
  /v1/matches.lastVisited:
    post:
      operationId: matches.lastVisited
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Match"
                nullable: true
  /v1/matches.list:
    post:
      operationId: matches.list
      tags: [matches]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchListReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Match"
                nullable: true
  /v1/matches.mergePlayers:
    post:
      operationId: matches.mergePlayers
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/mergePlayersReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/matches.movePositions:
    post:
      operationId: matches.movePositions
      tags: [matches]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_MatchMovePosition"
                nullable: true
  /v1/matches.moves:
    post:
      operationId: matches.moves
      tags: [matches]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/gameIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Move"
                nullable: true
  /v1/matches.movesByMatch:
    post:
      operationId: matches.movesByMatch
      tags: [matches]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Move"
                nullable: true
  /v1/matches.save:
    post:
      operationId: matches.save
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/matches.setLastVisitedPosition:
    post:
      operationId: matches.setLastVisitedPosition
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/lastVisitedReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/matches.swapPlayers:
    post:
      operationId: matches.swapPlayers
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/matches.update:
    post:
      operationId: matches.update
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchUpdateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/matches.updateComment:
    post:
      operationId: matches.updateComment
      tags: [matches]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchCommentReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/metadata.counts:
    post:
      operationId: metadata.counts
      tags: [metadata]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/storage_Counts"
  /v1/metadata.version:
    post:
      operationId: metadata.version
      tags: [metadata]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/versionResp"
  /v1/positions.delete:
    post:
      operationId: positions.delete
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/positions.epc:
    post:
      operationId: positions.epc
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/race_Result"
  /v1/positions.exists:
    post:
      operationId: positions.exists
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/existsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/existsResp"
  /v1/positions.explain:
    post:
      operationId: positions.explain
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/explainReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/engine_Explanation"
  /v1/positions.fromOGID:
    post:
      operationId: positions.fromOGID
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ogidReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/positions.fromXGID:
    post:
      operationId: positions.fromXGID
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/xgidReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/positions.fromXGP:
    post:
      operationId: positions.fromXGP
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/xgpReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/xgpResp"
  /v1/positions.legalMoves:
    post:
      operationId: positions.legalMoves
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/legalMovesReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/domain_LegalPlay"
  /v1/positions.list:
    post:
      operationId: positions.list
      tags: [positions]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/listReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/positions.listIds:
    post:
      operationId: positions.listIds
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/listReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: integer
                  format: int64
  /v1/positions.load:
    post:
      operationId: positions.load
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/positions.loadByIds:
    post:
      operationId: positions.loadByIds
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idsReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/domain_Position"
  /v1/positions.parseText:
    post:
      operationId: positions.parseText
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/parseTextReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/parser_Result"
  /v1/positions.reclassifyPhases:
    post:
      operationId: positions.reclassifyPhases
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/reclassifyResp"
  /v1/positions.save:
    post:
      operationId: positions.save
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/positions.similar:
    post:
      operationId: positions.similar
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/similarReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/storage_SimilarPosition"
  /v1/positions.update:
    post:
      operationId: positions.update
      tags: [positions]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/positionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/quiz.gradeChecker:
    post:
      operationId: quiz.gradeChecker
      tags: [quiz]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/quizCheckerReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/engine_QuizVerdict"
  /v1/quiz.gradeCheckerMove:
    post:
      operationId: quiz.gradeCheckerMove
      tags: [quiz]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/quizCheckerMoveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/engine_QuizVerdict"
  /v1/quiz.gradeCube:
    post:
      operationId: quiz.gradeCube
      tags: [quiz]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/quizCubeReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/engine_QuizVerdict"
  /v1/search.find:
    post:
      operationId: search.find
      tags: [search]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/searchFindReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Position"
                nullable: true
  /v1/search.intent:
    post:
      operationId: search.intent
      tags: [search]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/searchIntentReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/searchquery_Intent"
  /v1/search.parse:
    post:
      operationId: search.parse
      tags: [search]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/searchQueryReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/searchParseResp"
  /v1/search.query:
    post:
      operationId: search.query
      tags: [search]
      x-kind: custom
      responses:
        "200":
          description: OK
          description: >-
            Custom, hand-written handler; response shape not derived from this
            generator's source analysis (a file upload/download, or a route
            registered from a runtime table) — see internal/server for the actual
            implementation.
  /v1/searchHistory.deleteEntry:
    post:
      operationId: searchHistory.deleteEntry
      tags: [searchHistory]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/searchHistoryDeleteReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/searchHistory.list:
    post:
      operationId: searchHistory.list
      tags: [searchHistory]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/storage_SearchHistory"
                nullable: true
  /v1/searchHistory.save:
    post:
      operationId: searchHistory.save
      tags: [searchHistory]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/searchHistorySaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/session.clear:
    post:
      operationId: session.clear
      tags: [session]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/session.load:
    post:
      operationId: session.load
      tags: [session]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/storage_SessionState"
                nullable: true
  /v1/session.save:
    post:
      operationId: session.save
      tags: [session]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/sessionSaveReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/stats.compute:
    post:
      operationId: stats.compute
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/statsComputeReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/storage_StatsResult"
                nullable: true
  /v1/stats.dateRange:
    post:
      operationId: stats.dateRange
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/storage_StatsDateRange"
  /v1/stats.matchBadges:
    post:
      operationId: stats.matchBadges
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchBadgesReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/matchBadgesResp"
  /v1/stats.matchDetail:
    post:
      operationId: stats.matchDetail
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/storage_MatchDetailStats"
                nullable: true
  /v1/stats.playerNames:
    post:
      operationId: stats.playerNames
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/storage_PlayerFrequency"
  /v1/stats.playerTable:
    post:
      operationId: stats.playerTable
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/statsComputeReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/storage_PlayerRow"
  /v1/stats.positionIdsByMatch:
    post:
      operationId: stats.positionIdsByMatch
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idsResp"
  /v1/stats.positionIdsBySelection:
    post:
      operationId: stats.positionIdsBySelection
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/statsSelectionReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idsResp"
  /v1/stats.positionIdsByTournament:
    post:
      operationId: stats.positionIdsByTournament
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tournamentIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idsResp"
  /v1/stats.tournamentBadges:
    post:
      operationId: stats.tournamentBadges
      tags: [stats]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/tournamentBadgesResp"
  /v1/tournaments.addMatch:
    post:
      operationId: tournaments.addMatch
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tournamentMatchReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/tournaments.create:
    post:
      operationId: tournaments.create
      tags: [tournaments]
      x-kind: json
      x-idempotency-key: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tournamentCreateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/tournaments.delete:
    post:
      operationId: tournaments.delete
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/tournaments.get:
    post:
      operationId: tournaments.get
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Tournament"
                nullable: true
  /v1/tournaments.list:
    post:
      operationId: tournaments.list
      tags: [tournaments]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/listReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Tournament"
                nullable: true
  /v1/tournaments.matches:
    post:
      operationId: tournaments.matches
      tags: [tournaments]
      x-kind: stream
      x-streaming: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tournamentIDReq"
      responses:
        "200":
          description: OK
          content:
            application/x-ndjson:
              schema:
                description: One JSON object per line (NDJSON), each shaped as below.
                $ref: "#/components/schemas/domain_Match"
                nullable: true
  /v1/tournaments.removeMatch:
    post:
      operationId: tournaments.removeMatch
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/tournaments.reorderMatches:
    post:
      operationId: tournaments.reorderMatches
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/reorderMatchesReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/tournaments.setMatchByName:
    post:
      operationId: tournaments.setMatchByName
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/setMatchByNameReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/tournaments.tournamentOf:
    post:
      operationId: tournaments.tournamentOf
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/matchIDReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/domain_Tournament"
                nullable: true
  /v1/tournaments.update:
    post:
      operationId: tournaments.update
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tournamentUpdateReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/tournaments.updateComment:
    post:
      operationId: tournaments.updateComment
      tags: [tournaments]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/tournamentCommentReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/trash.count:
    post:
      operationId: trash.count
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/countResp"
  /v1/trash.deleteCollection:
    post:
      operationId: trash.deleteCollection
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/trash.deleteComment:
    post:
      operationId: trash.deleteComment
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/trash.deletePosition:
    post:
      operationId: trash.deletePosition
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
  /v1/trash.discard:
    post:
      operationId: trash.discard
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/okResp"
  /v1/trash.empty:
    post:
      operationId: trash.empty
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/trashEmptyReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/purgedResp"
  /v1/trash.list:
    post:
      operationId: trash.list
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/trashListReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/domain_TrashEntry"
                  nullable: true
  /v1/trash.restore:
    post:
      operationId: trash.restore
      tags: [trash]
      x-kind: json
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/idReq"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/idResp"
components:
  schemas:
    analysisSaveReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        analysis:
          $ref: "#/components/schemas/domain_PositionAnalysis"
          nullable: true
    cardIDReq:
      type: object
      properties:
        cardId:
          type: integer
          format: int64
    collPositionReq:
      type: object
      properties:
        collectionId:
          type: integer
          format: int64
        positionId:
          type: integer
          format: int64
    collPositionsReq:
      type: object
      properties:
        collectionId:
          type: integer
          format: int64
        positionIds:
          type: array
          items:
            type: integer
            format: int64
    collectionCreateReq:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
    collectionFilterReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        query:
          type: string
    collectionPositionsReq:
      type: object
      properties:
        collectionId:
          type: integer
          format: int64
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
    collectionReorderReq:
      type: object
      properties:
        collectionIds:
          type: array
          items:
            type: integer
            format: int64
    collectionUpdateReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        description:
          type: string
    commandSaveReq:
      type: object
      properties:
        command:
          type: string
    commentAddReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        text:
          type: string
    commentIDReq:
      type: object
      properties:
        commentId:
          type: integer
          format: int64
    commentSearchReq:
      type: object
      properties:
        query:
          type: string
    commentUpdateReq:
      type: object
      properties:
        commentId:
          type: integer
          format: int64
        text:
          type: string
    copyPositionReq:
      type: object
      properties:
        toCollectionId:
          type: integer
          format: int64
        positionId:
          type: integer
          format: int64
    countResp:
      type: object
      properties:
        count:
          type: integer
          format: int32
    deckCreateReq:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        sourceType:
          type: string
        sourceId:
          type: integer
          format: int64
        sourceCommand:
          type: string
    deckIDReq:
      type: object
      properties:
        deckId:
          type: integer
          format: int64
    deckParamsReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        requestRetention:
          type: number
          format: double
        maximumInterval:
          type: number
          format: double
        enableFuzz:
          type: boolean
        sessionLimit:
          type: integer
          format: int32
          nullable: true
    deckSyncPositionsReq:
      type: object
      properties:
        deckId:
          type: integer
          format: int64
        positionIds:
          type: array
          items:
            type: integer
            format: int64
    deckUpdateReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        description:
          type: string
    domain_AnkiDeck:
      type: object
      description: "Opaque: see the Go type domain.AnkiDeck (defined outside internal/server, not expanded here)."
    domain_AnkiDeckStats:
      type: object
      description: "Opaque: see the Go type domain.AnkiDeckStats (defined outside internal/server, not expanded here)."
    domain_AnkiForecastDay:
      type: object
      description: "Opaque: see the Go type domain.AnkiForecastDay (defined outside internal/server, not expanded here)."
    domain_AnkiRetention:
      type: object
      description: "Opaque: see the Go type domain.AnkiRetention (defined outside internal/server, not expanded here)."
    domain_AnkiReviewCard:
      type: object
      description: "Opaque: see the Go type domain.AnkiReviewCard (defined outside internal/server, not expanded here)."
    domain_AnkiReviewLog:
      type: object
      description: "Opaque: see the Go type domain.AnkiReviewLog (defined outside internal/server, not expanded here)."
    domain_Board:
      type: object
      description: "Opaque: see the Go type domain.Board (defined outside internal/server, not expanded here)."
    domain_CommentEntry:
      type: object
      description: "Opaque: see the Go type domain.CommentEntry (defined outside internal/server, not expanded here)."
    domain_Game:
      type: object
      description: "Opaque: see the Go type domain.Game (defined outside internal/server, not expanded here)."
    domain_ImportBatch:
      type: object
      description: "Opaque: see the Go type domain.ImportBatch (defined outside internal/server, not expanded here)."
    domain_LegalPlay:
      type: object
      description: "Opaque: see the Go type domain.LegalPlay (defined outside internal/server, not expanded here)."
    domain_Match:
      type: object
      description: "Opaque: see the Go type domain.Match (defined outside internal/server, not expanded here)."
    domain_MatchMovePosition:
      type: object
      description: "Opaque: see the Go type domain.MatchMovePosition (defined outside internal/server, not expanded here)."
    domain_Move:
      type: object
      description: "Opaque: see the Go type domain.Move (defined outside internal/server, not expanded here)."
    domain_Position:
      type: object
      description: "Opaque: see the Go type domain.Position (defined outside internal/server, not expanded here)."
    domain_PositionAnalysis:
      type: object
      description: "Opaque: see the Go type domain.PositionAnalysis (defined outside internal/server, not expanded here)."
    domain_SearchFilters:
      type: object
      description: "Opaque: see the Go type domain.SearchFilters (defined outside internal/server, not expanded here)."
    domain_StudyQueueEntry:
      type: object
      description: "Opaque: see the Go type domain.StudyQueueEntry (defined outside internal/server, not expanded here)."
    domain_TagCount:
      type: object
      description: "Opaque: see the Go type domain.TagCount (defined outside internal/server, not expanded here)."
    domain_Tournament:
      type: object
      description: "Opaque: see the Go type domain.Tournament (defined outside internal/server, not expanded here)."
    domain_TrashEntry:
      type: object
      description: "Opaque: see the Go type domain.TrashEntry (defined outside internal/server, not expanded here)."
    editPositionSaveReq:
      type: object
      properties:
        filterName:
          type: string
        editPosition:
          type: string
    engine_Explanation:
      type: object
      description: "Opaque: see the Go type engine.Explanation (defined outside internal/server, not expanded here)."
    engine_QuizVerdict:
      type: object
      description: "Opaque: see the Go type engine.QuizVerdict (defined outside internal/server, not expanded here)."
    excludePositionSaveReq:
      type: object
      properties:
        filterName:
          type: string
        excludePosition:
          type: string
    existsReq:
      type: object
      properties:
        zobrist:
          type: integer
          format: int64
    existsResp:
      type: object
      properties:
        id:
          type: integer
          format: int64
        found:
          type: boolean
    explainReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        played:
          type: string
    filterNameReq:
      type: object
      properties:
        filterName:
          type: string
    filterSaveReq:
      type: object
      properties:
        name:
          type: string
        command:
          type: string
    filterUpdateReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        command:
          type: string
    findByHashReq:
      type: object
      properties:
        hash:
          type: string
        canonicalHash:
          type: string
    findByHashResp:
      type: object
      properties:
        id:
          type: integer
          format: int64
        found:
          type: boolean
    forecastReq:
      type: object
      properties:
        deckId:
          type: integer
          format: int64
        days:
          type: integer
          format: int32
    gameIDReq:
      type: object
      properties:
        gameId:
          type: integer
          format: int64
    gameReq:
      type: object
      properties:
        game:
          $ref: "#/components/schemas/domain_Game"
          nullable: true
    idReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
    idResp:
      type: object
      properties:
        id:
          type: integer
          format: int64
    idsReq:
      type: object
      properties:
        ids:
          type: array
          items:
            type: integer
            format: int64
    idsResp:
      type: object
      properties:
        positionIds:
          type: array
          items:
            type: integer
            format: int64
    importReportReq:
      type: object
      properties:
        batchId:
          type: integer
          format: int64
        players:
          type: array
          nullable: true
          items:
            type: string
    importStudyQueueReq:
      type: object
      properties:
        batchId:
          type: integer
          format: int64
        players:
          type: array
          nullable: true
          items:
            type: string
        limit:
          type: integer
          format: int32
          nullable: true
    lastVisitedReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        positionIndex:
          type: integer
          format: int32
    legalMovesReq:
      type: object
      properties:
        position:
          $ref: "#/components/schemas/domain_Position"
          nullable: true
    listReq:
      type: object
      properties:
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
    matchBadgesReq:
      type: object
      properties:
        matchIds:
          type: array
          items:
            type: integer
            format: int64
    matchBadgesResp:
      type: object
      properties:
        badges:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/storage_MatchBadge"
    matchCommentReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        comment:
          type: string
    matchIDReq:
      type: object
      properties:
        matchId:
          type: integer
          format: int64
    matchListReq:
      type: object
      properties:
        playerName:
          type: string
        tournamentIds:
          type: array
          items:
            type: integer
            format: int64
        dateFrom:
          type: string
        dateTo:
          type: string
        matchLength:
          type: array
          items:
            type: integer
            format: int32
        sort:
          type: string
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
    matchSaveReq:
      type: object
      properties:
        match:
          $ref: "#/components/schemas/domain_Match"
          nullable: true
    matchUpdateReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        player1Name:
          type: string
        player2Name:
          type: string
        matchDate:
          type: string
    mergePlayersReq:
      type: object
      properties:
        names:
          type: array
          items:
            type: string
        canonical:
          type: string
    movePositionReq:
      type: object
      properties:
        fromCollectionId:
          type: integer
          format: int64
        toCollectionId:
          type: integer
          format: int64
        positionId:
          type: integer
          format: int64
    moveReq:
      type: object
      properties:
        move:
          $ref: "#/components/schemas/domain_Move"
          nullable: true
    ogidReq:
      type: object
      properties:
        ogid:
          type: string
    okResp:
      type: object
      properties:
        ok:
          type: boolean
    parseTextReq:
      type: object
      properties:
        text:
          type: string
    parser_Result:
      type: object
      description: "Opaque: see the Go type parser.Result (defined outside internal/server, not expanded here)."
    positionIDReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
    positionReq:
      type: object
      properties:
        position:
          $ref: "#/components/schemas/domain_Position"
          nullable: true
    purgedResp:
      type: object
      properties:
        purged:
          type: integer
          format: int32
    quizCheckerMoveReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        move:
          type: string
    quizCheckerReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        played:
          $ref: "#/components/schemas/domain_Board"
    quizCubeReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        action:
          type: string
    race_Result:
      type: object
      description: "Opaque: see the Go type race.Result (defined outside internal/server, not expanded here)."
    reclassifyResp:
      type: object
      properties:
        reclassified:
          type: integer
          format: int32
    reorderMatchesReq:
      type: object
      properties:
        tournamentId:
          type: integer
          format: int64
        matchIds:
          type: array
          items:
            type: integer
            format: int64
    repairResp:
      type: object
      properties:
        repaired:
          type: integer
          format: int32
    retentionReq:
      type: object
      properties:
        deckId:
          type: integer
          format: int64
    reviewCardReq:
      type: object
      properties:
        cardId:
          type: integer
          format: int64
        rating:
          type: integer
          format: int32
    reviewLogReq:
      type: object
      properties:
        deckId:
          type: integer
          format: int64
        limit:
          type: integer
          format: int32
    reviewsByGameTypeReq:
      type: object
      properties:
        since:
          type: string
    searchFindReq:
      type: object
      properties:
        filters:
          $ref: "#/components/schemas/domain_SearchFilters"
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
    searchHistoryDeleteReq:
      type: object
      properties:
        timestamp:
          type: integer
          format: int64
    searchHistorySaveReq:
      type: object
      properties:
        command:
          type: string
        position:
          type: string
        excludePosition:
          type: string
    searchIntentReq:
      type: object
      properties:
        text:
          type: string
    searchParseResp:
      type: object
      properties:
        filters:
          $ref: "#/components/schemas/domain_SearchFilters"
        diags:
          type: array
          nullable: true
          items:
            $ref: "#/components/schemas/searchQueryDiag"
        canonical:
          type: string
    searchQueryDiag:
      type: object
      properties:
        kind:
          type: string
        token:
          type: string
        message:
          type: string
    searchQueryReq:
      type: object
      properties:
        query:
          type: string
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
    searchquery_Intent:
      type: object
      description: "Opaque: see the Go type searchquery.Intent (defined outside internal/server, not expanded here)."
    sessionSaveReq:
      type: object
      properties:
        state:
          $ref: "#/components/schemas/storage_SessionState"
    setMatchByNameReq:
      type: object
      properties:
        matchId:
          type: integer
          format: int64
        tournamentName:
          type: string
    similarReq:
      type: object
      properties:
        positionId:
          type: integer
          format: int64
        limit:
          type: integer
          format: int32
    statsComputeReq:
      type: object
      properties:
        filter:
          $ref: "#/components/schemas/storage_StatsFilter"
    statsSelectionReq:
      type: object
      properties:
        filter:
          $ref: "#/components/schemas/storage_StatsFilter"
        selection:
          $ref: "#/components/schemas/storage_SelectionSpec"
    storage_Collection:
      type: object
      description: "Opaque: see the Go type storage.Collection (defined outside internal/server, not expanded here)."
    storage_Counts:
      type: object
      description: "Opaque: see the Go type storage.Counts (defined outside internal/server, not expanded here)."
    storage_Filter:
      type: object
      description: "Opaque: see the Go type storage.Filter (defined outside internal/server, not expanded here)."
    storage_MatchBadge:
      type: object
      description: "Opaque: see the Go type storage.MatchBadge (defined outside internal/server, not expanded here)."
    storage_MatchDetailStats:
      type: object
      description: "Opaque: see the Go type storage.MatchDetailStats (defined outside internal/server, not expanded here)."
    storage_PlayerFrequency:
      type: object
      description: "Opaque: see the Go type storage.PlayerFrequency (defined outside internal/server, not expanded here)."
    storage_PlayerRow:
      type: object
      description: "Opaque: see the Go type storage.PlayerRow (defined outside internal/server, not expanded here)."
    storage_SearchHistory:
      type: object
      description: "Opaque: see the Go type storage.SearchHistory (defined outside internal/server, not expanded here)."
    storage_SelectionSpec:
      type: object
      description: "Opaque: see the Go type storage.SelectionSpec (defined outside internal/server, not expanded here)."
    storage_SessionState:
      type: object
      description: "Opaque: see the Go type storage.SessionState (defined outside internal/server, not expanded here)."
    storage_SimilarPosition:
      type: object
      description: "Opaque: see the Go type storage.SimilarPosition (defined outside internal/server, not expanded here)."
    storage_StatsDateRange:
      type: object
      description: "Opaque: see the Go type storage.StatsDateRange (defined outside internal/server, not expanded here)."
    storage_StatsFilter:
      type: object
      description: "Opaque: see the Go type storage.StatsFilter (defined outside internal/server, not expanded here)."
    storage_StatsResult:
      type: object
      description: "Opaque: see the Go type storage.StatsResult (defined outside internal/server, not expanded here)."
    storage_TournamentBadge:
      type: object
      description: "Opaque: see the Go type storage.TournamentBadge (defined outside internal/server, not expanded here)."
    stringsResp:
      type: object
      properties:
        items:
          type: array
          items:
            type: string
    suspendCardReq:
      type: object
      properties:
        cardId:
          type: integer
          format: int64
        suspended:
          type: boolean
    tagsResp:
      type: object
      properties:
        tags:
          type: array
          items:
            $ref: "#/components/schemas/domain_TagCount"
        recommended:
          type: array
          items:
            type: string
    textResp:
      type: object
      properties:
        text:
          type: string
    tournamentBadgesResp:
      type: object
      properties:
        badges:
          type: object
          additionalProperties:
            $ref: "#/components/schemas/storage_TournamentBadge"
    tournamentCommentReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        comment:
          type: string
    tournamentCreateReq:
      type: object
      properties:
        name:
          type: string
        date:
          type: string
        location:
          type: string
    tournamentIDReq:
      type: object
      properties:
        tournamentId:
          type: integer
          format: int64
    tournamentMatchReq:
      type: object
      properties:
        tournamentId:
          type: integer
          format: int64
        matchId:
          type: integer
          format: int64
    tournamentUpdateReq:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        date:
          type: string
        location:
          type: string
    trashEmptyReq:
      type: object
      properties:
        olderThanDays:
          type: integer
          format: int32
    trashListReq:
      type: object
      properties:
        kind:
          type: string
          nullable: true
        limit:
          type: integer
          format: int32
        offset:
          type: integer
          format: int32
    versionResp:
      type: object
      properties:
        version:
          type: string
    xgidReq:
      type: object
      properties:
        xgid:
          type: string
    xgpReq:
      type: object
      properties:
        data:
          type: string
    xgpResp:
      type: object
      properties:
        position:
          $ref: "#/components/schemas/domain_Position"
          nullable: true
        analysis:
          $ref: "#/components/schemas/domain_PositionAnalysis"
          nullable: true
