# Follow-ups from the v2.0.0 optimization run

Items deferred from phases 00–06 that should be addressed in a future task sheet.

| # | Phase | Status | Item |
|---|---|---|---|
| 1 | 06 | done | `wails build` is now exercised on every tagged release: the CI matrix builds all 4 platforms (ubuntu-latest webkit2gtk-4.1, ubuntu-22.04 webkit2gtk-4.0, windows, macos) and `wails dev` compiles the same Go+frontend path locally. Verified green on the 0.26.1 tag build (run 27267855792). |
| 2 | 06 | open | GUI manual smoke test on a real migrated production DB not done — exercise every filter in the Search modal to confirm no regressions. Low priority; covered indirectly by the searchFilterService unit + round-trip tests (see `frontend/src/__tests__/searchFilterService.test.js`). |
| 3 | 05 | done | Added `idx_position_pip_diff ON position(pip_diff)` and `idx_position_dice ON position(dice_1, dice_2)`. PipWindow benchmark unchanged (planner correctly scans when >50% rows match); dice index IS used for OR-dice queries. |
| 4 | 05 | done | `BenchmarkSearch_WinGammonCombo` — query restructured to `p.id IN (SELECT position_id FROM analysis WHERE …)` and `idx_analysis_win_gammon` extended to `idx_analysis_win_gammon_covering (player1_win_rate, player1_gammon_rate, position_id)` (fiche-05 T3). EXPLAIN QUERY PLAN confirms the TEMP B-TREE is gone (`SEARCH analysis USING COVERING INDEX idx_analysis_win_gammon_covering`, then `SEARCH p USING INTEGER PRIMARY KEY`, no sort operator) — the plan-level diagnosis in this row was correct. Wall-clock gain on this fixture is modest (629 ms → ~500 ms, ~20 %, not the "→ ~100 ms" this row once guessed): the benchmark's filter (`w>0.55 g>0.2`) matches ~79 % of the 57 820 tournois positions, so it isn't selective enough for the sort removal to dominate — a narrower filter (`w>0.9 g>0.5`, ~fewer rows) measured about the same wall time, meaning Go-side position reconstruction, not the SQL sort, is now the bottleneck at this fixture size. Kept anyway: zero-risk, structurally correct, and should matter more on a larger disk-backed database or a genuinely selective filter. See fiche-05 Notes d'exécution for the full before/after and the E3 follow-on (dropped `idx_position_score`/`idx_analysis_win1`, redundant prefixes of `idx_position_score_cube`/the new covering index). |
| 5 | 02 | done | Added `idx_position_score_cube ON position(match_length, score_1, score_2, cube_value)` for score+cube combined filter. |
| 6 | 02 | done | Added `idx_game_match ON game(match_id)` — tournament subquery no longer SCANs game table. |
| 7 | — | open | `doc/source/_static/screenshot.png` (used by README.md) is stale — dated 2026-04-20, shows a Log tab that was since removed and predates the Stats panel. Fiche 10 (docs/aide-et-docs) dropped the README image reference rather than ship a misleading screenshot, since no graphical session was available to recapture it. Redo the capture on 0.32+ (`make dev`, open a populated database, screenshot the main window) and restore the README image. |
